import * as path from 'path';

const dir = path.join(__dirname, '..', 'test', '__fixtures__'); 
export const project1Path = path.join(dir, 'project-1');
export const project2Path = path.join(dir, 'project-2');
export const project3Path = path.join(dir, 'project-3');
